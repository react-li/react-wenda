## ReactJs pc wenda demo

mkdir wenda-react && cd wenda-react  && npn init

npm install --save-dev gulp gulp-browserify gulp-concat gulp-connect gulp-react lodash reactify

bower init 

bower install bootstrap --save
